class User {
    constructor(userId, name, email, orders, preferences) {
        this.userId = userId;
        this.name = name;
        this.email = email;
        this.orders = orders;
        this.preferences = preferences;
    }
}

export default User;